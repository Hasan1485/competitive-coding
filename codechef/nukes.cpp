#include <bits/stdc++.h>
#define ll long long int
using namespace std;
int start_up()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    return 0;
}
int static r = start_up();
int main()
{
    int a, n, k;
    cin >> a >> n >> k;
    vector<int> arr;
    for (int i = 0; i < k; i++)
    {
        arr.at(i) = 0;
    }
    
    return 0;
}