/*
author-sesky_crocdile
once a crocodile
always a CROCODILE
*/
#include<bits/stdc++.h>
#define int long long int
using namespace std;
int start_up() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    return 0;
}
int static r = start_up();
void solve() {
    int l, r, d, u;
    cin >> l >> r >> d >> u;
    int curh, curv;
    cin >> curh >> curv;
    int x1, y1, x2, y2;
    cin >> x1 >> y1 >> x2 >> y2;
    
}
signed main() {
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}